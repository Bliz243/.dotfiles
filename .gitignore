# Local overrides (machine-specific)
.zsh/99-local.zsh
.gitconfig.local

# Claude Code - machine-specific and runtime
.claude/settings.local.json
.claude/state/

# Codex CLI - machine-specific (config.toml has project trust paths)
# .codex/config.toml is a template; actual config lives in ~/.codex/

# Playwright CLI session artifacts
.playwright-cli/

# OS
.DS_Store

# Editor
*.swp
*.swo
*~
