# Local overrides (machine-specific)
.zsh/99-local.zsh
.gitconfig.local

# Claude Code - machine-specific and runtime
.claude/settings.local.json
.claude/state/

# Codex CLI - machine-specific (config.toml has project trust paths + MCP IPs)
# Tracked template is .codex/config.toml.example; real config lives in ~/.codex/
.codex/config.toml

# Project-specific Codex skills (kept local, reconsider per-project)
.codex/skills/unity-mcp-skill/
.codex/skills/unity-uitoolkit-runtime/

# Playwright CLI session artifacts
.playwright-cli/

# OS
.DS_Store

# Editor
*.swp
*.swo
*~
